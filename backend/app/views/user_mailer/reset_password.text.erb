Olá, <%= @user.name %>!

Recebemos uma solicitação para redefinir a senha da sua conta no TrackRide.

Acesse o link abaixo para criar uma nova senha:
<%= @reset_url %>

O link expira em 2 horas. Se você não pediu isso, ignore este email — sua senha continua a mesma.
