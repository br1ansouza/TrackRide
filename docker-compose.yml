services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_USER: trackride
      POSTGRES_PASSWORD: trackride_dev
      POSTGRES_DB: trackride_development
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  pgdata:
